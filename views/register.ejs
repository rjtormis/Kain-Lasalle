<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Kain LaSalle</title>
    <meta name="keywords" content="" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <link rel="stylesheet" type="text/css" href="css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="css/style.css" />
    <link rel="stylesheet" href="css/responsive.css" />
    <link rel="stylesheet" href="css/jquery.mCustomScrollbar.min.css" />
    <link
      rel="stylesheet"
      href="https://netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css"
    />
    <link
      href="https://fonts.googleapis.com/css?family=Poppins:400,700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/owl-carousel/1.3.3/owl.carousel.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="header_section">
      <div class="container-fluid">
        <!-- ... (existing header content) ... -->
      </div>
    </div>

    <center>
      <div class="login">
        <form action="/register" method="POST" id="registrationForm">
          <h1>Register</h1>

          <div class="input-box">
            <select id="registrationType" name="type" required>
              <option value="" disabled selected>Select Registration Type</option>
              <option value="student">Student</option>
              <option value="faculty">Faculty</option>
            </select>
          </div>

          <div class="input-box">
            <input type="email" id="email" name="email" placeholder="School Email" required />
          </div>

          <div class="input-box">
            <input type="text" id="fullName" name="fullName" placeholder="Full Name" required />
          </div>

          <div class="input-box">
            <input type="text" id="userName" name="userName" placeholder="userName" required />
          </div>

          <div class="input-box">
            <input type="password" id="password" name="password" placeholder="Password" required />
          </div>

          <button type="submit" class="btnLogin">Register</button>

          <div class="reglink">
            <p>Already have an account? <a href="/">Login</a></p>
          </div>

          <!-- New code for "Login as guest" -->
          <div class="input-box">
            <p>Login as guest <a href="homepage.html">here</a></p>
          </div>
        </form>
      </div>
    </center>

    <!-- <script>
      document.getElementById("registrationForm").addEventListener("submit", function (event) {
        event.preventDefault(); // Prevent the default form submission

        var registrationType = document.getElementById("registrationType").value;
        var schoolEmail = document.getElementById("schoolEmail").value;
        var fullName = document.getElementById("fullName").value;
        var username = document.getElementById("username").value;
        var password = document.getElementById("password").value;

        if (registrationType && schoolEmail && fullName && username && password) {
          // Use fetch API to send data to the server
          fetch("/register", {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify({
              registrationType: registrationType,
              schoolEmail: schoolEmail,
              fullName: fullName,
              username: username,
              password: password,
            }),
          })
            .then((response) => {
              if (!response.ok) {
                throw new Error(response.statusText);
              }
              return response.json(); // Parse response as JSON
            })
            .then((data) => {
              alert(data.message); // Display the response message from the server

              // Check for specific messages and redirect accordingly
              if (data.message.includes("Redirecting to login")) {
                window.location.href = "/index.html";
              }
            })
            .catch((error) => {
              console.error("Error during registration:", error);
            });
        } else {
          alert("Please fill in all the required fields.");
        }
      });
    </script> -->
  </body>
</html>
